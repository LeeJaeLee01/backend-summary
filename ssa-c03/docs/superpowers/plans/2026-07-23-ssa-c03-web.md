# SAA-C03 Web App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship CRA React TS frontend + Express APIs for practice/study/exam, bilingual EN-primary display, notes, and progress for a single default user.

**Architecture:** Extend Express/Mongo with `users`, `user_notes`, `user_question_state`, `exam_sessions`; CRA app in `ssa-c03/web` proxies to `:3010`.

**Tech Stack:** Express, MongoDB driver, cors, Create React App (TS), yarn, React Router v6

**Spec:** `docs/superpowers/specs/2026-07-23-ssa-c03-web-design.md`

---

## File map

### Backend create
- `src/services/userService.js` — ensure default user, preferences, stats
- `src/services/noteService.js` — get/upsert notes
- `src/services/progressService.js` — get/patch progress, list filters
- `src/services/sessionService.js` — create/answer/reveal/finish sessions + grading
- `src/routes/me.js`, `notes.js`, `progress.js`, `sessions.js`

### Backend modify
- `src/db/mongo.js` — collections + indexes
- `src/index.js` — cors + mount routes + ensureDefaultUser
- `src/config.js` — `corsOrigin`
- `src/services/importService.js` — list/get with optional user state merge
- `src/routes/questions.js` — filters + include
- `.env.example`, `README.md`, `package.json` (cors)

### Frontend create (`web/`)
- CRA scaffold (yarn + typescript)
- `src/api/client.ts`, `types.ts`
- `src/styles/tokens.css`, `App.css`
- Pages: Dashboard, QuestionList, QuizPlayer, SessionRunner, SessionReview, NotesIndex, Settings
- Components: BilingualText, OptionCard, NoteEditor, ModePills, ProgressBadge

---

### Task 1: Mongo collections + default user + CORS

- [ ] Add COLLECTIONS + indexes in `mongo.js`
- [ ] Add `userService.ensureDefaultUser`
- [ ] Install `cors`; wire in `index.js`
- [ ] Smoke: `GET /me` returns default user

### Task 2: Notes + Progress APIs

- [ ] Implement note/progress services + routes
- [ ] Smoke: PUT note, PATCH progress, GET list filters

### Task 3: Sessions API + grade helper

- [ ] Implement session create/answer/reveal/finish
- [ ] Smoke: practice submit grades correctly

### Task 4: Extend questions list/get

- [ ] Support `bookmarked`, `status`, `q`, `include=state,note`

### Task 5: Scaffold CRA `web/`

- [ ] `yarn create react-app web --template typescript`
- [ ] Add react-router-dom, proxy `3010`, base layout + CSS tokens

### Task 6: FE API + Dashboard + List

- [ ] Typed client; dashboard stats; question browser with filters

### Task 7: Quiz player (Practice + Study + notes + bilingual)

- [ ] Main learning screen with keyboard shortcuts + autosave notes

### Task 8: Exam session + review + settings + notes index

- [ ] Timed exam, finish/review, preferences, notes list

### Task 9: Docs + smoke verification

- [ ] Update README; curl + browser smoke checklist

---

**Execution:** Inline in this session (user requested immediate implementation). Commit only if user asks.
